{
  "name": "knack-install",
  "version": "1.0.0",
  "description": "installs repository as a node module",
  "main": "./dist/index.js",
  "bin": "./dist/index.js",
  "scripts": {
    "pub": "npm publish --access=public",
    "build": "rm -rf dist && microbundle --tsconfig tsconfig.json --no-sourcemap && npm publish --access=public",
    "test": "PACKAGE_NAME=KnackLab"
  },
  "keywords": [],
  "author": "Israel Ayokunnu",
  "license": "ISC",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "dotenv": "^16.0.3"
  },
  "dependencies": {
    "envalid": "^7.3.1",
    "util": "^0.12.4",
    "yargs": "^17.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/KnackLab/knack-install"
  }
}
